/**
 * Maximum page size for paginated queries.
 *
 * Used to prevent excessive data fetching and ensure consistent
 * pagination behavior across all dashboards.
 */
export const MAX_PAGE_SIZE = 100;
